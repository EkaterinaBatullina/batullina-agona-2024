spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/agona_db
    username: Ekaterina
    password: eka_rina16
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
  cloud:
    consul:
      discovery:
        hostname: localhost
        instance-id: client-service
        health-check-path: /actuator/health
      host: localhost
      port: 8500

management:
  endpoints:
    web:
      exposure:
        include: health